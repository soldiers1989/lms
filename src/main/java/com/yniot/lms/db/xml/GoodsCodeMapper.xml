<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.yniot.lms.db.dao.GoodsCodeMapper">

    <update id="setState">
        update biz_goods_code bgc
        inner JOIN biz_order_goods bog on bog.id = bgc.order_goods_id
         set
        <if test="state ==0">
            bgc.used = 0,
            bgc.order_id = 0,
            bgc.order_goods_id = 0,
        </if>
        <if test="state !=0">
            bgc.used = 1,
            bgc.order_id = bog.order_id,
            bgc.order_goods_id = #{orderGoodsId},
        </if>
        where unique_code in
        <foreach collection="list" item="item" index="index" open="(" separator="," close=")">
            #{item}
        </foreach>
    </update>
</mapper>